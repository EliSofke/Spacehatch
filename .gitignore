# Secrets — never commit tokens or the session secret
.env
*.pem

# Build output and dependencies
backend/node_modules/
backend/dist/

# Editor / OS noise
.vscode/
.DS_Store

# In-codespace bridge dependencies (installed by postCreateCommand)
.devcontainer/terminal-bridge/node_modules/

# Auth worker (no deps, but ignore if installed locally)
auth-worker/node_modules/

# Browser-SSH spike: SDKs loaded from esm.sh at runtime
frontend-browser-ssh/vendor/
frontend-browser-ssh/bundle-src/
test/node_modules/
node_modules/

# gh-in-WASM build artifacts (built in CI from gh-wasm-src/ via gh-wasm.yml)
frontend-gh-wasm/gh.wasm
frontend-gh-wasm/wasm_exec.js
